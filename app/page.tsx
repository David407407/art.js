import Image from "next/image";
import { jetbrains } from "./layout";

export default function Home() {
  return (
    <main className="bg-[#2D3440] w-full min-h-screen">
      <h1 className={`${jetbrains.className} text-5xl text-center font-bold bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 text-transparent bg-clip-text leading-normal`}>Art.jsasdasdsd</h1>
    </main>
  );
}
